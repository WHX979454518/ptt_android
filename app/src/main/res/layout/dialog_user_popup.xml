<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="@dimen/unit_one"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/userPopup_icon"
        tools:background="@color/blue"
        android:layout_marginRight="@dimen/unit_one"
        android:layout_width="@dimen/user_list_icon_size"
        android:layout_height="@dimen/user_list_icon_size" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_gravity="center"
        android:layout_weight="1"
        android:layout_width="0dp"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/userPopup_name"
            style="@style/Text.Button"
            tools:text="Title"
            android:singleLine="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/userPopup_level"
            style="@style/Text.Caption"
            tools:text="Subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <Button
        android:id="@+id/userPopup_call"
        style="@style/Widget.AppCompat.Button.Borderless"
        android:text="@string/call"
        android:drawableLeft="@drawable/ic_voice"
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</LinearLayout>